---
interface Props {
	images: string[];
	alt: string;
	autoAdvance?: number;
}

const { images, alt, autoAdvance } = Astro.props;
---

<div class="product-carousel-wrapper">
<div class="product-carousel relative w-full aspect-[4/3] overflow-hidden bg-gray-100 rounded-t-lg" data-images={JSON.stringify(images)} data-alt={alt} data-auto-advance={autoAdvance ?? 0}>
	<div class="carousel-container relative w-full h-full">
		{images.map((image, index) => (
			<div
				class={`carousel-slide absolute inset-0 transition-opacity duration-500 cursor-pointer ${
					index === 0 ? 'opacity-100' : 'opacity-0'
				}`}
				data-slide-index={index}
			>
				<img
					src={image}
					alt={`${alt} - Image ${index + 1}`}
					class="w-full h-full object-cover"
					loading={index === 0 ? 'eager' : 'lazy'}
					fetchpriority={index === 0 ? 'high' : 'auto'}
					decoding="async"
					data-image-index={index}
				/>
			</div>
		))}
	</div>

	<!-- Navigation arrows -->
	{images.length > 1 && (
		<>
			<button
				class="carousel-prev absolute left-2 top-1/2 -translate-y-1/2 bg-white/80 hover:bg-white text-gray-800 p-2 rounded-full shadow-lg transition-all z-10"
				aria-label="Previous image"
			>
				<svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
					<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
				</svg>
			</button>
			<button
				class="carousel-next absolute right-2 top-1/2 -translate-y-1/2 bg-white/80 hover:bg-white text-gray-800 p-2 rounded-full shadow-lg transition-all z-10"
				aria-label="Next image"
			>
				<svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
					<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
				</svg>
			</button>

			<!-- Dots indicator -->
			<div class="absolute bottom-2 left-1/2 -translate-x-1/2 flex gap-2 z-10">
				{images.map((_, index) => (
					<button
						class={`carousel-dot w-2 h-2 rounded-full transition-all ${
							index === 0 ? 'bg-white' : 'bg-white/50'
						}`}
						data-dot-index={index}
						aria-label={`Go to image ${index + 1}`}
					></button>
				))}
			</div>
		</>
	)}
</div>

<!-- Image Viewer Modal -->
<div class="image-viewer-modal fixed inset-0 bg-black/90 z-50 hidden items-center justify-center pb-24" role="dialog" aria-modal="true" aria-label="Image viewer">
	<!-- Close button -->
	<button
		class="image-viewer-close absolute top-4 right-4 bg-white/10 hover:bg-white/20 text-white p-3 rounded-full transition-all z-20"
		aria-label="Close image viewer"
	>
		<svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
			<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
		</svg>
	</button>

	<!-- Previous button -->
	<button
		class="image-viewer-prev absolute left-4 top-1/2 -translate-y-1/2 bg-white/10 hover:bg-white/20 text-white p-4 rounded-full transition-all z-20"
		aria-label="Previous image"
	>
		<svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
			<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
		</svg>
	</button>

	<!-- Next button -->
	<button
		class="image-viewer-next absolute right-4 top-1/2 -translate-y-1/2 bg-white/10 hover:bg-white/20 text-white p-4 rounded-full transition-all z-20"
		aria-label="Next image"
	>
		<svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
			<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
		</svg>
	</button>

	<!-- Image container -->
	<div class="image-viewer-container relative max-w-[90vw] max-h-[80vh] flex items-center justify-center">
		<img
			class="image-viewer-image max-w-full max-h-[80vh] object-contain"
			alt=""
			src=""
		/>
	</div>

	<!-- Thumbnail strip -->
	{images.length > 1 && (
		<div class="absolute bottom-0 left-0 right-0 flex justify-center gap-2 overflow-x-auto py-3 px-4 bg-gradient-to-t from-black/60 to-transparent">
			{images.map((image, index) => (
				<button
					class={`image-viewer-thumb flex-shrink-0 w-14 h-14 rounded overflow-hidden transition-all ${
						index === 0 ? 'ring-2 ring-white opacity-100' : 'opacity-50'
					}`}
					data-thumb-index={index}
					aria-label={`View image ${index + 1}`}
				>
					<img src={image} alt="" class="w-full h-full object-cover" loading="lazy" />
				</button>
			))}
		</div>
	)}
</div>
</div>

<script>
	import './ProductCarousel.ts';
</script>
